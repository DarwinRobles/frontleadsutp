server {
  listen 80;
  server_name utpdemo.evolucionista.dev;

  root /usr/share/nginx/html;

  index index.html index.htm;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location /assets/ {
        try_files $uri =404;
    }

    error_page 404 /index.html;
}